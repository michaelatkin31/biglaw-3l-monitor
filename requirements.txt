# Core runtime deps.
requests>=2.31,<3
PyYAML>=6.0,<7

# Needed at runtime by the `browser` fetcher (ats_type: browser) for the handful
# of firms whose openings only exist in a JS-rendered DOM (Hunton, Baker McKenzie,
# Buchanan Ingersoll, ...). The workflow runs `playwright install chromium`.
# If Chromium isn't installed, those firms fail per-firm (logged) and the rest run.
playwright>=1.44
